# Strategy Configs

`defaults.yaml` documents the strategy parameters currently defined in
`src/brain/strategy_selector.py`.

The Python module is still the runtime source of truth. Keep this folder in
sync when strategy parameters change, or wire a future loader to make YAML the
source of truth.
